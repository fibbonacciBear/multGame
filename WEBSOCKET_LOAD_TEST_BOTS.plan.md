# WebSocket Load-Test Bots (Merged)

The load-test bot plan is now merged into:

- `GRACEFUL_DRAIN_AND_GAMEMODE_TRANSFER_PLAN.md`

See the **WebSocket Load-Test Harness Plan** section there for architecture, files, lifecycle, and validation scenarios aligned with graceful drain and intermission transfer behavior.

