apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-server
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: game-server
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
